// User moduuli
function getName(name){
    return name + " lives in "
} 

function getLocation(location) {
    return location + " and was born on "
}

const bd = "01.01.1111"

module.exports = {
    getName, getLocation, bd
}